agent_limitation — The `insert_changelog_entry` tool created spurious `CHANGELOG.md` files twice on a project that uses `CHANGELOG.rst`, wasting tool calls on orphan-file cleanup. Added changelog-format detection (`CHANGELOG.rst`, `CHANGELOG.md`, `CHANGELOG` checked in order) and fixed two RST insertion bugs: a wrong index when appending a new unreleased section, and a false heading match on any line containing "unreleased".
