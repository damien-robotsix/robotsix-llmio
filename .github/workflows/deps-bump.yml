name: Deps Bump

on:
  schedule:
    - cron: "0 6 * * 1"   # every Monday 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deps-bump:
    uses: damien-robotsix/robotsix-mill/.github/workflows/deps-bump.yml@a2b9f2f986f60ad8ba5ff89fa44ed85986e19cdf # main
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
