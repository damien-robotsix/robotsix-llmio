"""Shared OpenRouter transport constants.

Single source of truth for the OpenRouter REST endpoint, imported by both the
inference provider and the provider-cost adapter so the URL is maintained in
one place.
"""

_DEFAULT_BASE_URL = "https://openrouter.ai/api/v1"
